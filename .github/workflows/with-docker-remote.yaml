name: Remote reusable workflow

on: workflow_dispatch

jobs:
  build:
    uses: PythonF4DE/reusable-workflows/.github/workflows/build.yaml@main
    with:
      push: true
    secrets: inherit